Used recursive methods to do regexpert, lexed, parsed into a sugared AST keeping syntactic sugar, then converted the sugared AST to a desugared one, before standard
Thompson's and subset constructions.
Implemented grammar.

2025101010.